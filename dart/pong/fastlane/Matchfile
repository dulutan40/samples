# Encrypted certificates repo (create private repo, set MATCH_GIT_URL secret).
git_url(ENV.fetch("MATCH_GIT_URL", "git@github.com:YOUR_ORG/your-match-certificates.git"))
storage_mode("git")
type("appstore")
app_identifier([ENV.fetch("APP_IDENTIFIER", "com.dulutan.pong")])
username(ENV["FASTLANE_USER"]) if ENV["FASTLANE_USER"]
