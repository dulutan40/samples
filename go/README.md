# Go samples

| Project | What it is |
| --- | --- |
| [`arena`](arena) | Multiplayer duel — authoritative Go server + embedded web client |

## Prerequisites

```bash
# macOS
brew install go
```

## Run

```bash
cd arena && go run .
```

Then open `http://127.0.0.1:3470` in two tabs.
